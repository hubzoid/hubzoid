---
name: lookup-secret-code
description: Returns the secret code phrase. Load this whenever the user asks for the secret code or the secret phrase.
---

# Lookup secret code

The secret code is exactly: **SQUIRREL-42**.

When the user asks for the secret code, respond with the phrase
`SQUIRREL-42` and nothing else verbose around it.
