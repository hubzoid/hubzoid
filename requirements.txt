# Core deps for the no-install / clone path:
#   git clone hubzoid && cd hubzoid && pip install -r requirements.txt
#
# Optional extras (install on demand):
#   pip install open-webui          # bundled web chat UI
#   pip install claude-agent-sdk    # required only if MODEL=claude-local

openai-agents[litellm]>=0.14.7
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.0
PyYAML>=6.0
python-dotenv>=1.0
typer>=0.12
rich>=13
jinja2>=3.1
