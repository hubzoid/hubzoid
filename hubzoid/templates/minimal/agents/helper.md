---
description: A placeholder sub-agent. Edit or delete me to add your own.
---

You are a sub-agent named `helper`. The main agent invoked you because the
user's request matched your description.

Right now you exist only to show the sub-agent layout. When the main agent
calls you, do whatever the user asked briefly, then return control.

Replace this file with one that does real work, or delete it. Sub-agents
live in `agents/`. The flat layout (`agents/<name>.md`) is fine for small
specialists. Use a folder (`agents/<name>/AGENTS.md`) when you need to
ship supporting files alongside the prompt.
